{
  "name": "@yame/dev",
  "version": "0.0.1",
  "description": "Development module for yame.",
  "main": "dist/main.js",
  "author": "Trixt0r",
  "license": "ISC",
  "dependencies": {
    "electron-reload": "^1.2.5"
  },
  "scripts": {
    "build": "git merge ng2 && npm install && tsc"
  },
  "devDependencies": {
    "typescript": "^3.1.1"
  }
}
